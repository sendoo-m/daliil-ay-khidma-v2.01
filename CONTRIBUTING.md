# المساهمة في دليل أي خدمة

## الفروع

- `master`: النسخة المستقرة فقط.
- `agent/<description>` أو `feature/<description>`: تطوير جديد.
- `fix/<description>`: إصلاح محدد.
- `docs/<description>`: تغييرات توثيق فقط.

لا يتم التعديل المباشر على `master`.

## خطوات العمل

1. ابدأ من أحدث نسخة من `master`.
2. أنشئ فرعًا محدود النطاق.
3. نفذ التغيير مع الاختبارات المناسبة.
4. حدث التوثيق عندما يتغير السلوك أو العقد أو المعمارية.
5. افتح Pull Request واضحًا.
6. لا تدمج قبل نجاح الفحوصات والمراجعة.

## رسائل commits

استخدم رسائل قصيرة تصف النتيجة، مثل:

```text
fix: validate favorite ownership
docs: document authentication flow
feat: add business opening status
```

يفضل عدم جمع تغييرات غير مترابطة في commit واحد.

## متطلبات Pull Request

يجب أن يوضح الوصف:

- ما الذي تغير؟
- لماذا تغير؟
- أثره على المستخدم أو المطور.
- طريقة الاختبار.
- أي مخاطر أو أعمال لاحقة.

## فحوصات Backend

شغل ما ينطبق على التغيير:

```bash
python manage.py check
python manage.py test
pytest
flake8
```

لا يلزم تشغيل كل أداة إذا لم تكن مهيأة فعليًا، لكن يجب توثيق ما تم تشغيله.

## فحوصات Flutter

```bash
cd mobile/dalil_app
flutter pub get
flutter gen-l10n
dart format --set-exit-if-changed lib test
flutter analyze
flutter test
```

للتغييرات المؤثرة على البناء:

```bash
flutter build web --release
flutter build apk --debug
```

## قواعد التوثيق

- `README.md` للتعريف والتشغيل السريع.
- `PROJECT_CONTEXT.md` للحالة والقرارات الحالية.
- `ROADMAP.md` للمراحل المستقبلية.
- `CHANGELOG.md` للتغييرات المهمة.
- `docs/architecture/` للمعمارية.
- `docs/api/` لعقود API.
- `docs/adr/` للقرارات الهندسية التي يصعب التراجع عنها.

لا تكرر نفس التفاصيل في عدة ملفات دون حاجة.

## تغييرات قاعدة البيانات

- أضف migration مع أي تغيير Model.
- لا تعدل migrations منشورة إلا لسبب موثق.
- راجع القيم الافتراضية وعمليات ترحيل البيانات.
- اختبر الترقية من قاعدة بيانات قائمة، وليس قاعدة جديدة فقط.

## الأمان والخصوصية

- لا ترفع مفاتيح أو أسرار أو ملفات بيئة.
- لا تسجل JWT أو كلمات المرور أو بيانات شخصية في السجلات.
- تحقق من ملكية المورد في عمليات التعديل والحذف.
- تحقق من نوع وحجم الملفات المرفوعة.
- لا توسع CORS أو الصلاحيات دون سبب موثق.

## معيار الإنجاز

لا تعتبر المهمة مكتملة لمجرد أن الكود يعمل محليًا. يجب أن تكون:

- محدودة النطاق.
- مختبرة بما يناسبها.
- متوافقة مع الأنماط الحالية.
- موثقة عند الحاجة.
- قابلة للمراجعة والرجوع.